[
  {
    "name": "pubsub_project_id",
    "type": "string",
    "description": "The project ID containing the Enforcer pubsub topic.",
    "default": "",
    "required": true
  },
  {
    "name": "sink_project_id",
    "type": "string",
    "description": "The project ID where logs will be exported from.",
    "default": "",
    "required": true
  }
]
